<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tikarajbista.w1l4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ShoppingCategory" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".BeautyActivity" />
        <activity android:name=".ClothingActivity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".FoodDetail" />
        <activity android:name=".ElectronicsActivity" />
        <activity android:name=".ElectronicDetail"></activity>
    </application>

</manifest>